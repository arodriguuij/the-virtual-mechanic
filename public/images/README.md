# public/images

`scott-addict.webp` is the real product photo shown on the Dashboard's "Mi
bicicleta actual" card (`app/page.tsx`, `BikeHeroCard`). Transparent
background, catalog-style side profile — that's why it's rendered with
`object-contain` at a fixed height rather than filling a background panel.

`scott-addict.png` is an earlier/unused copy — nothing in the app references
it. Safe to delete once you're sure you don't need it as a source file.
